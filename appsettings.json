{
  "ConnectionStrings": {
    /*
      The MySQL string is: "server=localhost;database=DATABASENAME;user=USERNAME;password=PASSWORD"
      In MySQL:
        1) Create a new schema named "darntheluck"
        2a) Create a new user named "darntheluck"
        2b) Add Entry to give darntheluck user access to darntheluck schema
        2c) Select All privileges for this user

      NOTE: The connection string is now stored in a class that does not get stored in the Git repository
    */
    "DefaultConnection": "THIS_STRING_IS_NO_LONGER_VALID"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
